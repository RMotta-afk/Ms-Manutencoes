/* $(document).ready(function () {
  $(window).bind("scroll", function () {
    var gap = 50;
    if ($(window).scrollTop() > gap) {
      $(".nav-bar").addClass("active");
      $("li").addClass("color-hover");
    } else {
      $(".nav-bar").removeClass("active");
      $("li").removeClass("color-hover");
    }
  });
});
*/
